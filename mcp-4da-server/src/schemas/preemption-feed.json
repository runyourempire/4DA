{
  "name": "preemption_feed",
  "description": "Get forward-looking intelligence alerts about upcoming risks, breaking changes, and ecosystem shifts affecting your stack.\n\nSynthesizes signal chains, dependency health alerts, and knowledge gaps into actionable preemption intelligence. Use before dependency updates or when planning work.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "urgency_filter": {
        "type": "string",
        "description": "Filter by minimum urgency: critical, high, medium, watch. Default: all",
        "enum": ["critical", "high", "medium", "watch"]
      },
      "limit": {
        "type": "number",
        "description": "Maximum alerts to return. Default: 10"
      }
    }
  }
}
