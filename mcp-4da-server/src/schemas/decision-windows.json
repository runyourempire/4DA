{
  "name": "decision_windows",
  "description": "View and manage decision windows — time-bounded opportunities detected from content intelligence.\n\nList open windows sorted by urgency, mark windows as acted on with an outcome, or close windows that are no longer relevant.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "enum": ["list", "act", "close"],
        "description": "Action to perform. Default: list"
      },
      "window_id": {
        "type": "number",
        "description": "Window ID (required for 'act' or 'close' actions)"
      },
      "outcome": {
        "type": "string",
        "description": "Outcome description when acting on a window"
      }
    }
  }
}
