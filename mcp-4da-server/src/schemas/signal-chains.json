{
  "name": "signal_chains",
  "description": "Get causal signal chains connecting related events over time.\n\nTracks evolving stories across sources — e.g., a security vulnerability first mentioned on HN, then an advisory on GitHub, then a fix release. Chains link related signals into narratives.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "resolution": {
        "type": "string",
        "enum": ["open", "resolved", "expired", "all"],
        "description": "Filter by chain resolution status. Default: open"
      },
      "min_priority": {
        "type": "string",
        "enum": ["critical", "high", "medium", "low"],
        "description": "Minimum chain priority to include. Default: low"
      }
    }
  }
}
