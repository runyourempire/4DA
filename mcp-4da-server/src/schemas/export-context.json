{
  "name": "export_context_packet",
  "description": "Generate a portable context packet for session handoff between AI agents or tools.\n\nExports the user's current 4DA context including open signals, saved items, and active state into a self-contained packet that another session can consume.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "include_signals": {
        "type": "boolean",
        "description": "Include open signals. Default: true"
      },
      "include_saved": {
        "type": "boolean",
        "description": "Include saved items. Default: true"
      }
    }
  }
}
