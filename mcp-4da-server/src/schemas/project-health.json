{
  "name": "project_health",
  "description": "Project health radar for dependency freshness and security.\n\nAssesses the health of your scanned projects by analyzing dependency counts, package freshness, and cross-referencing with content signals for security advisories or breaking changes.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "project_path": {
        "type": "string",
        "description": "Specific project path to check. Omit for all projects."
      }
    }
  }
}
