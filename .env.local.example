# 4DA local environment variables — copy to .env.local (gitignored)
# DO NOT commit .env.local
#
# 4DA currently requires NO build-time environment variables. It ships with
# no third-party telemetry or crash reporting, so there is nothing to configure
# here. This file is kept as the place to document any future opt-in build flags.
