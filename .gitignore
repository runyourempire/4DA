# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
*.tsbuildinfo

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code artifacts
.claude/sessions/
.claude/backups/
.claude/memory.db
.claude/settings.local.json
.claude/analyzer-state.json
.claude/knowledge/
*.local.md

# ONNX model cache (binary files)
src-tauri/.fastembed_cache/

# ONNX Runtime DLL (bundled separately)
src-tauri/ort-runtime/

# Tauri build artifacts
src-tauri/target/
src-tauri/fourda-macros/target/
src-tauri/fourda-macros/Cargo.lock

# SQLite database files
data/*.db
data/*.db-shm
data/*.db-wal

# Runtime usage stats (changes every session)
data/usage.json

# User settings (contains API keys and local paths)
data/settings.json

# License validation cache (runtime state)
data/license_cache.json

# Model registry cache (auto-refreshed from LiteLLM)
data/model_registry.json

# Environment files (may contain secrets from `vercel env pull`)
.env*
!.env.example

# ONNX models
src-tauri/models/

# MCP memory server build
mcp-memory-server/dist/
mcp-memory-server/node_modules/
mcp-memory-server/memory.db

# Development scratch files
dev.sh
dev-light.sh
dev-tools/
test-context/
ton-library/
nul

# Python scratch scripts
src-tauri/src/_*.py
src-tauri/src/_*.txt

# Session artifacts (not project docs)
MISSION_ACCOMPLISHED.md
CONFIDENCE_SCORE_IMPLEMENTATION.md
SHIP_READINESS_VERIFICATION.md
README-MARKETING.md

# CLI workspace (not ready)
cli/

# Raw screen recordings (large, not for repo)
assets/raw/

# Playwright E2E test artifacts
e2e-results/
e2e-report/
e2e-screenshots/
test-results/
playwright-report/

# Test scratch files
test_stderr.txt
test_stdout.txt
tmp_test.txt

# Database backups
data/*.backup.*

# Merch build outputs (regenerate with: node merch-print-ready/build.mjs)
merch-print-ready/png/
merch-print-ready/upscaled/

# Test screenshots (artifacts)
test-screenshots/

.claude/wisdom/
.claude/worktrees/
site/.vercel/

# Runtime screenshots (Lightshot dumps, not project assets)
data/screenshot-*.png

# Runtime signal token
data/signal_terminal_token.txt

# Coverage report (generated by vitest --coverage)
coverage/

# Eleventy build output
site/_site/
target-test/

# GAME compiler shader artifacts (embedded in .js components)
src/lib/game-components/*.wgsl
src/lib/game-components/*.frag

# 4DA World (separate repo)
4DA-WORLD-DOCUMENTS/
4DA-WORLD-PLAN.md
4da-world/
relay/target/

# Crate placeholder (name reservation only)
crates-placeholder/

# Legal deed files (contain personal ABN and address — private)
docs/legal/DEED-*

# Private key / certificate files (never commit)
*.key
*.pem
*.p12
*.pfx
*.jks
*.keystore
credentials.json
service-account*.json

# MCP server build artifacts
mcp-4da-server/dist/
mcp-4da-server/node_modules/
mcp-streets-server/dist/
mcp-streets-server/node_modules/

# AWE wisdom database (lives at %APPDATA%/awe/, not in repo)
# Listed for defense-in-depth
wisdom.db

# Zero-byte junk files (editor/tool artifacts)
Color
Sdf
end
target.property
[outputs...]
.apple-cert-temp/

# Sensitive data defense-in-depth
*.secret
*.secrets
*.credential*
**/secrets/
**/credentials/
*.gpg
*.asc
id_rsa*
id_ed25519*
id_ecdsa*
known_hosts
.netrc
.npmrc
!.npmrc.example
.pypirc
