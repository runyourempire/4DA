// SPDX-License-Identifier: FSL-1.1-Apache-2.0
//! Validation Runner — placeholder module.
//!
//! The `run_scoring_validation` Tauri command was removed (ghost command —
//! never registered in invoke_handler, never called from frontend).
//! Persona-based validation helpers that were only reachable through that
//! command were removed with it.
