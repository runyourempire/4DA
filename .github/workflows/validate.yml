name: Validate

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1
  NODE_OPTIONS: '--max-old-space-size=4096'

jobs:
  frontend:
    name: Frontend
    runs-on: [self-hosted, windows, 4da]
    steps:
      - uses: actions/checkout@v4
        with:
          clean: false

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Check file sizes
        run: node scripts/check-file-sizes.cjs --ci

      - name: TypeScript type check
        run: pnpm run typecheck

      - name: ESLint
        run: pnpm run lint

      - name: Run tests with coverage
        run: pnpm run test -- --coverage

      - name: Build
        run: pnpm run build

  mcp-server:
    name: MCP Server
    needs: frontend
    runs-on: [self-hosted, windows, 4da]
    steps:
      - uses: actions/checkout@v4
        with:
          clean: false

      - name: Install dependencies
        shell: bash
        run: cd mcp-4da-server && pnpm install --frozen-lockfile

      - name: Build
        shell: bash
        run: cd mcp-4da-server && pnpm run build

      - name: Run tests
        shell: bash
        run: cd mcp-4da-server && pnpm run test

  rust:
    name: Rust
    needs: mcp-server
    runs-on: [self-hosted, windows, 4da]
    steps:
      - uses: actions/checkout@v4
        with:
          clean: false

      - name: Check formatting
        shell: bash
        run: cd src-tauri && cargo fmt --check

      - name: Clippy lints
        shell: bash
        run: cd src-tauri && cargo clippy -- -D warnings

      - name: Cargo check
        shell: bash
        run: cd src-tauri && cargo check

      - name: Run unit tests
        shell: bash
        run: cd src-tauri && cargo test --all

      - name: Verify Rust test count floor
        shell: bash
        run: |
          cd src-tauri
          OUTPUT=$(cargo test --all 2>&1)
          COUNT=$(echo "$OUTPUT" | grep -oP '\d+ passed' | tail -1 | grep -oP '\d+')
          echo "Rust tests: $COUNT"
          if [ "$COUNT" -lt 650 ]; then
            echo "::error::Test count dropped below 650 (got $COUNT)"
            exit 1
          fi

      - name: Run migration tests
        shell: bash
        run: cd src-tauri && cargo test --test migration_tests

      - name: Run pipeline integration tests
        shell: bash
        run: cd src-tauri && cargo test --test pipeline_integration

      - name: Run source resilience tests
        shell: bash
        run: cd src-tauri && cargo test --test source_resilience

      - name: Build
        shell: bash
        run: cd src-tauri && cargo build
